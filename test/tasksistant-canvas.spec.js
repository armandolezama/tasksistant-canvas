import { fixture, assert } from "@open-wc/testing";
import '../tasksistant-canvas';

describe('Suite cases', () => {
  it('First Test', async () => {
      const element = await fixture('<tasksistant-canvas></tasksistant-canvas>');
  })
})